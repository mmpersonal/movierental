SearchService for searching for movies based on attributes ( movie name, title, actor name ,language, rating etc).
this will be free text search and as well targeted search. will use Solr (elastic search) for efficient/faster search.
will be indexing movie name, title , person etc that are top search attributes for movie rental
